/**
 * JavaFX pages, navigation, and reusable presentation components.
 *
 * <p>This layer renders application state and forwards user intent to
 * application use cases. UI visibility may reflect permissions, but this layer
 * must never replace domain or application authorization checks.</p>
 */
package com.company.incidentdesk.ui;
