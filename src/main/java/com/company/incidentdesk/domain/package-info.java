/**
 * Core business concepts and rules for Incident Desk.
 *
 * <p>This layer is organized by business capability, such as accounts and
 * incidents. Domain code must remain independent of JavaFX, file persistence,
 * and startup concerns so its validation, lifecycle, and authorization rules
 * can be tested deterministically.</p>
 */
package com.company.incidentdesk.domain;
