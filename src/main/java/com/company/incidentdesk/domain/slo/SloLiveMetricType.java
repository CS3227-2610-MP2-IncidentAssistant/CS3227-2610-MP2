package com.company.incidentdesk.domain.slo;

/** Per-incident live SLO metric measured for one incident's current lifecycle position. */
public enum SloLiveMetricType {
    TIME_TO_CLAIM,
    TIME_IN_PROGRESS
}
