/**
 * Application startup, configuration, and dependency assembly.
 *
 * <p>This layer verifies the runtime, initializes storage and process locking,
 * constructs concrete dependencies, and launches JavaFX. It is the outermost
 * composition boundary and must delegate business behavior to the application
 * and domain layers.</p>
 */
package com.company.incidentdesk.startup;
